# Path to the project directory to mount at /workspace.
# Windows-host path if Docker Desktop's WSL2 integration is used, e.g.:
# PROJECT_DIR=C:\path\to\your\project
# WSL2-native path if the Docker daemon runs inside WSL2 itself, e.g.:
# PROJECT_DIR=/home/you/projects/your-project
PROJECT_DIR=
