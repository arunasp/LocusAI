{
  "name": "locusai-local-bash-desktop-extension",
  "version": "3.0.0",
  "private": true,
  "description": "Desktop-extension entry point for LocusAI's local-bash MCP proxy. Uses @modelcontextprotocol/sdk's own transports directly, in-process -- no subprocess, no mcp-remote -- so a stale session after a server-container rebuild can be detected and recovered instead of hanging until Desktop's own client timeout.",
  "main": "index.js",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3"
  }
}
